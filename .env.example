# API Configuration
ASPNETCORE_ENVIRONMENT=Development
API_PORT=5264

# Database Configuration
POSTGRES_DB=micro_service_1
POSTGRES_USER=postgres
POSTGRES_PASSWORD=dinushA@727
DB_PORT=5433

# Migrations (set to true to automatically apply pending EF Core migrations on container startup)
APPLY_MIGRATIONS=true

# JWT Settings
JWT_KEY=replace-with-a-long-random-secret-at-least-32-characters
JWT_ISSUER=ServiceCenterApi
JWT_AUDIENCE=ServiceCenterApiClients
JWT_EXPIRY_MINUTES=60
